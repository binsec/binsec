(include_subdirs no)

(library
 (public_name binsec.sse)
 (name binsec_sse)
 (modules :standard)
 (modules_without_implementation types)
 (flags
  (:standard -open Binsec_kernel))
 (libraries
  toml
  binsec.kernel
  binsec.symbolic
  binsec.sse.loader
  binsec.script))
